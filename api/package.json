{
  "name": "deCODE",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@types/mongoose": "^5.10.5",
    "mongoose": "^5.12.6",
    "socket.io": "^4.0.1"
  },
  "scripts": {
    "build": "rm -rf ./build && tsc --build tsconfig.json",
    "start": "yarn build && node ./build/index.js",
    "start:dev": "export MONGODB_SERVER=127.0.0.1 && export MONGODB_DATABASE=decode1 && nodemon"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
