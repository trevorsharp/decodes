{
  "name": "deCODES",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "mongoose": "^5.12.6",
    "socket.io": "^4.0.1"
  },
  "scripts": {
    "build": "rm -rf ./build && tsc --build tsconfig.json",
    "start": "yarn build && node ./build/index.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}